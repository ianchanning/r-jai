[package]
name = "r-jai"
version = "0.1.0"
edition = "2021"

[dependencies]
rustix = { version = "0.38", features = ["fs", "mount", "process"] }
nix = { version = "0.27", features = ["fs", "mount", "process", "user"] }
clap = { version = "4.4", features = ["derive"] }
anyhow = "1.0"
